/**
 * @constant {number} DUPLICATE_ITEM
 * This constant represents the MongoDB error code for duplicate key errors.
 * Used to handle scenarios where a unique constraint is violated.
 */
const DUPLICATE_ITEM = 11000;

export { DUPLICATE_ITEM };
